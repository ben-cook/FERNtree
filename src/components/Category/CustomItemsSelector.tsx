function CustomItemsSelector(props: {
  customFields: string[];
  setCustomFields: (s: string[]) => void;
}) {
  console.log(props.customFields);
  return <div></div>;
}

export default CustomItemsSelector;
