import { Box, CircularProgress } from "@material-ui/core";

const Loading = () => {
  return (
    <Box style={{ margin: 50 }}>
      <CircularProgress />
    </Box>
  );
};

export default Loading;
